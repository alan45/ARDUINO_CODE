module pulse(
    input  clk,      // clock
    input  rst,      // reset
    output clock[4]  // output
  ) {
  
  dff counter[14](.clk(clk), .rst(rst));

  always {
    clock = counter.q[10:7];
    counter.d = counter.q + 1;
  }
}