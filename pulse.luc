module pulse(
    input  clk,      // clock
    input  rst,      // reset
    output clock[8]  // output
  ) {
  
  dff counter[28](.clk(clk), .rst(rst));

  always {
    clock = counter.q[20:13];
    counter.d = counter.q + 1;
  }
}